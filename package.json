{
  "name": "pipeline",
  "description": "Be less busy, less mistake in Salesforce metadata deploy",
  "version": "0.1.0",
  "private": false,
  "dependencies": {
    "archiver": "^2.1.1",
    "async": "^2.6.1",
    "events": "^3.0.0",
    "extract-zip": "^1.6.7",
    "github-api": "^3.0.0",
    "jsforce": "^1.8.4",
    "make-dir": "^1.3.0",
    "moment": "^2.22.2",
    "querystring": "^0.2.0",
    "raven": "^2.6.3",
    "request": "^2.87.0",
    "rimraf": "^2.6.2",
    "sfdc-generate-package": "^2.2.1",
    "url": "^0.11.0",
    "uuid": "^3.2.1"
  },
  "keywords": [],
  "author": "Xiaoan Lin<admin@xgeek.net>",
  "license": "MIT",
  "scripts": {
    "start": "electron ./src",
    "build-macOS": "electron-packager ./src --out ./build Pipeline --platform=darwin --arch=x64 --overwrite --icon=./src/img/app-icon.icns",
    "build-windows": "electron-packager ./src --out ./build Pipeline --platform=win32 --arch=x64 --overwrite --icon=./src/img/app-icon.icns",
    "build": "electron-packager ./src Pipeline --all --out ./build --overwrite --icon=./src/img/app-icon.icns"
  },
  "devDependencies": {
    "electron": "^2.0.0",
    "electron-packager": "^12.0.2",
    "electron-prebuilt": "^1.4.13"
  }
}
